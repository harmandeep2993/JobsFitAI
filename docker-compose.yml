services:
  jobfitai:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - ./backend/data:/app/backend/data
      - ./backend/resumes:/app/backend/resumes
      - ./backend/logs:/app/backend/logs
    env_file:
      - ./backend/.env
    restart: unless-stopped
