services:
  jobfitai:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
      - ./resumes:/app/resumes
      - ./logs:/app/logs
    env_file:
      - .env
    restart: unless-stopped
