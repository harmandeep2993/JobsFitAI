services:
  jobfitai:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - ./data:/app/data
      - ./resumes:/app/resumes
      - ./logs:/app/logs
    env_file:
      - .env
    restart: unless-stopped
